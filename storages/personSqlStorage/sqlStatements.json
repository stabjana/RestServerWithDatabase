{
    "getAll":[
        "select id, firstname, lastname, department, salary",
        "from employee"
    ],
    "getOne":[
        "select id, firstname, lastname, department, salary",
        "from employee where id = ?"
    ],
    "insert":[
        "insert into employee (id, firstname, lastname, department, salary)",
        "values (?, ?, ?, ?)"
    ],
    "update":[
        "update employee",
        "set firstname = ?, lastname = ?, department = ?, salary = ? where id = ?"
    ],
    "remove":[ "delete from employee where id = ?"],
        "search":{
            "id":["select id, firstname, lastname, department, salary",
                "from employee where id = ?"],
            "firstname":["select id, firstname, lastname, department, salary",
                "from employee where firstname = ?"],
            "lastname":["select id, firstname, lastname, department, salary",
                "from employee where lastname = ?"],
            "department":["select id, firstname, lastname, department, salary",
                "from employee where department = ?"],
            "salary":["select id, firstname, lastname, department, salary",
                "from employee where salary = ?"]
        },
        "primary_key":"id",
        "resource":"employees"
}